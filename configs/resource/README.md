# Resource Configs

This directory is reserved for reusable dataset and model resource templates,
following the `ERASURE-main/configs/resource` convention.

Use resource configs for:

- dataset source templates
- predictor templates
- reusable split recipes
- shared evaluation presets
